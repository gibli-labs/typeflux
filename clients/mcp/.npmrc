# typeflux-mcp publishes to PUBLIC npmjs.org (Phase-0 decision, #326) so that
# `npx -y typeflux-mcp` works unauthenticated — see publishConfig in package.json.
# The control-plane client is inlined into dist/ at build time from the local
# monorepo build (esbuild alias), so no authenticated registry is involved.
