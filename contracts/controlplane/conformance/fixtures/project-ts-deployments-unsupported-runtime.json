{
  "editions": {
    "ts-cp": {
      "note": "The unsupported-runtime 501 mirrors: the TS server cannot resolve the python-runtime foreign project py-project (#620/#621 \u2014 surfaced by the console e2e: the TS deployments routes were serving [] for foreign projects where Python answers 501).",
      "request": {
        "method": "GET",
        "path": "/api/v1/projects/py-project/deployments"
      },
      "response": {
        "body": {
          "error": "UnsupportedRuntime",
          "message": "project 'py-project' declares runtime 'python', which this server cannot resolve (supported: typescript); resolution-dependent operations are unavailable for it"
        },
        "status": 501
      }
    }
  },
  "name": "project-ts-deployments-unsupported-runtime",
  "request": {
    "method": "GET",
    "path": "/api/v1/projects/ts-project/deployments"
  },
  "response": {
    "body": {
      "error": "UnsupportedRuntime",
      "message": "project 'ts-project' declares runtime 'typescript', which this server cannot resolve (supported: python); resolution-dependent operations are unavailable for it"
    },
    "status": 501
  }
}
